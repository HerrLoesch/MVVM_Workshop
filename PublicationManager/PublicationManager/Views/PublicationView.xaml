<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:PublicationManager.ViewModels"
             xmlns:views="clr-namespace:PublicationManager.Views"
             xmlns:MVVM="clr-namespace:PublicationManager.MVVM" xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             x:Class="PublicationManager.Views.PublicationView"
             mc:Ignorable="d" 
             MVVM:ViewModelLocator.AutoWireViewModel="True"
             d:DataContext="{d:DesignInstance IsDesignTimeCreatable=True, Type={x:Type viewModels:PublicationViewModel}}" 
             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.Resources>
        <MVVM:ExtendedBooleanToVisibilityConverter x:Key="HideButtonsDuringEditModeConverter" Mode="Inverse"/>
        <MVVM:ExtendedBooleanToVisibilityConverter x:Key="ShowButtonsDuringEditModeConverter" Mode="Normal"/>
        <DataTemplate x:Key="PresentationTemplate">
            <views:PublicationDetailsPresentationView/>
        </DataTemplate>
        <DataTemplate x:Key="EditTemplate">
            <views:PublicationDetailsEditView />
        </DataTemplate>
    </UserControl.Resources>
    <i:Interaction.Triggers>
        <i:EventTrigger>
            <i:InvokeCommandAction Command="{Binding InitializationCommand, Mode=OneTime}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <DockPanel>
        <DockPanel>
            <DockPanel DockPanel.Dock="Top" Margin="4">
                <TextBlock DockPanel.Dock="Left"><Run Text="Filter:"/></TextBlock>
                <TextBox Margin="2,0,0,0"/>
            </DockPanel>

            <TextBlock DockPanel.Dock="Bottom" Margin="4"><Run Text="no elements found"/></TextBlock>

            <ListBox DockPanel.Dock="Bottom" ItemsSource="{Binding Publications}" SelectedItem="{Binding SelectedPublication}" Width="150" Margin="4,2">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Title}" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
                </ListBox>
        </DockPanel>

        <!-- actual content -->
        <Grid DockPanel.Dock="Right">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>
            <ContentControl HorizontalAlignment="Center" 
                            VerticalAlignment="Center" 
                            Content="{Binding SelectedPublication}" >
                <ContentControl.Style>
                    <Style TargetType="{x:Type ContentControl}">

                        <!-- Default Template -->
                        <Setter Property="ContentTemplate" Value="{StaticResource PresentationTemplate}" />

                        <!-- Triggers to change Template -->
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsInEditMode}" Value="true">
                                <Setter Property="ContentTemplate" Value="{StaticResource EditTemplate}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ContentControl.Style>
            </ContentControl>

            <!-- Buttonbar -->
            <StackPanel Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Bottom" Orientation="Horizontal">
                <Button Width="90" Margin="4" Content="Cancel" Command="{Binding CancelEditModeCommand}" Visibility="{Binding IsInEditMode, Converter={StaticResource ShowButtonsDuringEditModeConverter}}" />
                <Button Width="90" Margin="4" Content="Edit" Command="{Binding StartEditModeCommand}" Visibility="{Binding IsInEditMode, Converter={StaticResource HideButtonsDuringEditModeConverter}}" />
            </StackPanel>

        </Grid>
    </DockPanel>
</UserControl>
